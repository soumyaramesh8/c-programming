#include<stdio.h>
int main()
{
    int num;
    printf("Select Option:");
    scanf("%d",&num);
    if(num==80)
    {
        printf("Hello World");
    }
    if(num==100) 
    {
        printf("welcome all");
    }
    return 0;
}