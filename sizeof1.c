#include <stdio.h>
int main()
{
    printf(" storage size for int :%d",sizeof(int));
    printf("\n storage size for char : %d"),sizeof(char);
    printf("\n storage size for unsigned char : %d",sizeof(unsigned char));
    printf("\n storage size for signed char :%d",sizeof(signed char));
    printf("\n storage size for unsigned int : %d",sizeof(unsigned int));
    printf("\n storage size for short : %d",sizeof(short));
    printf("\n storage size for unsigned short :%d",sizeof(unsigned short));
    printf("\n storage size for long :%d",sizeof(long));
    printf("\n storage size for unsigned long : %d",sizeof(unsigned long));
    return 0;
}