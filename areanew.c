#include<stdio.h>
#define LENGTH 5
#define WIDTH 2
#define NEWLINE 'a'
int main()
{
    int area;
    area=LENGTH*WIDTH;
    printf("value of area is: %d",area);
    printf("%c",NEWLINE);
    return 0;
}